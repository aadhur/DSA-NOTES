# lambda (1)

lambda input: expression

It’s a keyword in python to create small, anonymous (nameless) one line functions that returns an value automatically

```
is_even_odd = lambda x:"Even"if x %2 ==0else"Odd"
print(is_even_odd(7))

add = lambda x, y: x+y
print(2,3)
```